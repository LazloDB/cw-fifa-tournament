export interface player {
  name: string,
  points: number,
  played: number,
  goals: number,
  goals_against: number,
  goal_difference: number,
  wins: number,
  draws: number,
  losses: number,
}
